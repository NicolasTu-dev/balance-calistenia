export const metadata = { title: "Ingresar", description: "Ingresá con tu email para acceder a tu panel de alumno." };

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
